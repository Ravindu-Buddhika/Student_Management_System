spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_management_system?createDatabaseIfNotExist = true
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show_sql : true